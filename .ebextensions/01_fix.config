container_commands:
  01_fix_everything:
    command: |
      rm -rf /var/www/html
      cp -r /var/app/current /var/www/html
      chown -R apache:apache /var/www/html
      chmod -R 755 /var/www/html
      setenforce 0 || true
      systemctl restart httpd || true
    ignoreErrors: true
